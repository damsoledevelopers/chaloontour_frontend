import logo from '../public/chalo-on-tour-e1766686260447.png';

/** Static import so the logo is emitted with the build (works even if `public/` is missing on the host). */
export const appLogo = logo;

/** Direct path for use in contexts where the import object doesn't work (e.g., static export metadata). */
export const appLogoPath = '/chalo-on-tour-e1766686260447.png';
